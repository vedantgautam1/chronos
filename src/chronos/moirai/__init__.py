"""Moirai — the validation gauntlet of Chronos. Spec: docs/SPEC_MOIRAI.md.

The judge that exists to reject almost every strategy fed to it. This package
is under active build (see MOIRAI_BUILD_BRIEF.md). `statistics.py` — the
pure-math core every Moira calls for anything statistical — is the first
module to land (Phase 1).
"""
