"""Phase 6 — append-only record store + persistent monotonic trial counter.

The invariants (append-only, hypothesis-required, every-trial-counted) are
NOT deferred; only storage sophistication is. Not built yet — Phase 0 placeholder.
"""
